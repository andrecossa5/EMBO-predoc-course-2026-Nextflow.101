// Nextflow configuration for Pipeline 1

// Manifest
manifest {
    name = 'pipeline-1-basic'
    author = 'EMBO Course 2026'
    homePage = 'https://github.com/embo/nextflow-course'
    description = 'Basic Nextflow concepts pipeline'
    mainScript = 'main.nf'
    nextflowVersion = '>=1.0'
    version = '1.0.0'
}

// Default parameters
params {
    input = "${baseDir}/data/sample.fa"
    outdir = "results"
}

// Process execution configuration
process {
    // Default settings for all processes
    cpus = 1
    memory = '1 GB'
    time = '1h'

    // Process-specific settings (if needed)
    withName: splitSequences {
        cpus = 1
        memory = '512 MB'
    }

    withName: reverseSequences {
        cpus = 1
        memory = '256 MB'
    }
}
